# Vector Knowledge Base Dependencies

# Sentence Transformers for embeddings
sentence-transformers==2.2.2

# Document processing libraries
PyMuPDF==1.23.8  # For PDF text extraction
python-docx==0.8.11  # For DOCX text extraction
pandas==2.1.4  # For CSV processing

# Vector operations
numpy==1.24.3

# Note: pgvector extension should be enabled in PostgreSQL/Supabase
# The sentence-transformers model 'all-MiniLM-L6-v2' will be downloaded automatically
# and cached locally (~90MB for the model)

